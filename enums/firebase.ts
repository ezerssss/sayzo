export enum FirestoreCollectionName {
    USERS = "users",
    SKILL_MEMORIES = "skill-memories",
    SESSIONS = "sessions",
    AUTH_SESSIONS = "auth_sessions",
    AUTH_CODES = "auth_codes",
    REFRESH_TOKENS = "refresh_tokens",
}
